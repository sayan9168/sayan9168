[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sayanox-cyberresearch-engine"
version = "0.1.0"
description = "Defensive cybersecurity research and source-analysis toolkit"
requires-python = ">=3.10"

[project.scripts]
cyberresearch = "cyberresearch_engine.__main__:main"
